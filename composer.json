{
    "name": "jtrw/php-make",
    "description": "Simple collections of make commands to increase productivity",
    "type": "library",
    "license": "MIT",
    "authors": [
        {
            "name": "Nil Borodulia",
            "email": "nil.borodulia@gmail.com"
        }
    ],
    "require": {},
    "scripts": {
        "post-install-cmd": [
            "php -r \"$source = __DIR__ . '/Makefile_Template.mk'; $destination = getcwd() . '/Makefile'; if (!file_exists($destination) && file_exists($source)) { copy($source, $destination); }\""
        ]
    }
}
